'use client';
import { useEffect,useState } from 'react';import { useRouter } from 'next/navigation';import { CounterWorkspace,TablesWorkspace } from './components/order-workspace.js';import ShiftWorkspace from './components/shift-workspace.js';import RecipeManager from './components/recipe-manager.js';import { Dashboard,Expenses,Inventory,MenuAdmin,Reports,Settings,Station,Users,TableAdmin } from './components/manager-modules.js';
const roleTabs={waiter:[['tables','▦','Tables']],cashier:[['counter','＋','New Order'],['tables','▦','Tables'],['shift','◷','My Shift'],['menu','☷','Menu'],['recipes','≋','Recipes']],manager:[['dashboard','⌂','Dashboard'],['counter','＋','New Order'],['tables','▦','Tables'],['tableadmin','▦','Table Setup'],['shift','◷','Shifts'],['inventory','◫','Inventory'],['recipes','≋','Recipes'],['expenses','↘','Expenses'],['reports','⌁','Finance'],['users','♙','Users'],['menu','☷','Menu'],['settings','⚙','Settings'],['bar','◉','Bar'],['kitchen','◌','Kitchen']]};
export default function Workspace({initialUser}){const router=useRouter(),[data,setData]=useState(null),[tab,setTab]=useState(initialUser.role==='waiter'?'tables':initialUser.role==='cashier'?'counter':'dashboard'),[error,setError]=useState('');async function load(){const r=await fetch('/api/bootstrap',{cache:'no-store'});if(r.status===401){router.replace('/login');return}const d=await r.json().catch(()=>({}));if(!r.ok){setError(d.error||'Could not load POS');return}setError('');setData(d)}useEffect(()=>{void load();const timer=setInterval(()=>void load(),7000);return()=>clearInterval(timer)},[]);async function logout(){await fetch('/api/auth/logout',{method:'POST'});router.replace('/login');router.refresh()}if(!data)return <div className="loading">Loading Cocktaillo POS…</div>;const tabs=roleTabs[data.user.role];return <div className="app"><header className="topbar"><div className="topBrand"><div className="miniMark">C</div><strong>Cocktaillo</strong><span>{data.user.role}</span></div><div className="topActions"><button>عربي</button><button onClick={logout}>Log out</button></div></header><aside className="desktopNav">{tabs.map(([id,icon,label])=><button key={id} className={`navItem ${tab===id?'active':''}`} onClick={()=>setTab(id)}><span>{icon}</span>{label}</button>)}</aside><main className="main">{error&&<div className="error">{error}</div>}<ActiveModule tab={tab} data={data} reload={load}/></main><nav className="mobileNav">{tabs.map(([id,icon,label])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}><span>{icon}</span>{label}</button>)}</nav></div>}
function ActiveModule({tab,data,reload}){if(tab==='counter')return <CounterWorkspace data={data} reload={reload}/>;if(tab==='tables')return <TablesWorkspace data={data} reload={reload} waiter={data.user.role==='waiter'}/>;if(tab==='tableadmin')return <TableAdmin data={data} reload={reload}/>;if(tab==='shift')return <ShiftWorkspace data={data} reload={reload}/>;if(tab==='dashboard')return <Dashboard data={data}/>;if(tab==='inventory')return <Inventory data={data} reload={reload}/>;if(tab==='recipes')return <RecipeManager data={data} reload={reload}/>;if(tab==='expenses')return <Expenses data={data} reload={reload}/>;if(tab==='reports')return <Reports data={data}/>;if(tab==='users')return <Users data={data} reload={reload}/>;if(tab==='menu')return <MenuAdmin data={data} reload={reload}/>;if(tab==='settings')return <Settings data={data} reload={reload}/>;if(tab==='bar'||tab==='kitchen')return <Station data={data} reload={reload} station={tab}/>;return null}
