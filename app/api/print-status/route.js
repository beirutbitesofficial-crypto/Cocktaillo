import {NextResponse} from 'next/server';import {getUser,allow} from '../../../lib/auth.js';import {mutateState} from '../../../lib/store.js';
export async function POST(request){const user=await getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});if(!allow(user,'cashier','manager'))return NextResponse.json({error:'Not allowed.'},{status:403});const b=await request.json().catch(()=>({}));try{const result=await mutateState(s=>{const t=s.tickets.find(x=>x.id===b.ticket_id);if(!t)throw new Error('Ticket not found.');const now=new Date().toISOString();if(b.action==='attempt'){t.print_status='attempted';t.print_attempts=Number(t.print_attempts||0)+1;t.last_print_at=now}if(b.action==='confirm'){t.print_status='confirmed';t.print_confirmed_at=now;t.print_confirmed_by=user.name}if(b.action==='retry'){t.print_status='pending'}return {ticket:t}});return NextResponse.json(result)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Print status failed.'},{status:400})}}
