import {NextResponse} from 'next/server';import {getUser,allow} from '../../../lib/auth.js';import {readState} from '../../../lib/store.js';export async function GET(){const user=await getUser();if(!allow(user,'manager'))return NextResponse.json({error:'Manager only.'},{status:403});const s=await readState();return NextResponse.json({deliveries:(s.report_deliveries||[]).slice(-100).reverse()},{headers:{'Cache-Control':'no-store'}})}
