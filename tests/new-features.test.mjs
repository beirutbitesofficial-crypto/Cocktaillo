import test from 'node:test';import assert from 'node:assert/strict';import {refundAmountForItems} from '../lib/refunds.js';import {buildDailyWorkbook} from '../lib/daily-report.js';test('partial refund respects quantity and discount ratio',()=>{const state={refunds:[],receipts:[{order_id:'o1',totals:{subtotal_equivalent_cents:2000,total_equivalent_cents:1800}}]},orders:[],recipes:[],inventory:[]},order={id:'o1',lines:[{id:'l1',menu_item_id:'m1',name_en:'Item',quantity:2,price_cents:1000,addons:[]}]};const r=refundAmountForItems(state,order,[{line_id:'l1',quantity:1}],89500);assert.equal(r.amount_cents,900)});test('daily report creates xlsx buffer',()=>{const state={settings:{exchange_rate:89500},orders:[],receipts:[],refunds:[],expenses:[],purchases:[],shifts:[],inventory:[],audit:[],recipes:[],tables:[]};const r=buildDailyWorkbook(state,new Date());assert.ok(r.buffer.length>100);assert.match(r.filename,/\.xlsx$/)})
